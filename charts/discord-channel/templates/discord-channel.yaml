apiVersion: shopops.com/v1
kind: DiscordChannel
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  # The operator creates a Discord channel with this name, adds a webhook, and
  # writes the webhook URL into Secret discord-{{ .Release.Name }}.
  shopName: {{ .Values.shopName | quote }}
