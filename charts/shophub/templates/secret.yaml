apiVersion: v1
kind: Secret
metadata:
  name: {{ include "shophub.credentialsSecret" . }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  username: {{ .Values.database.username | quote }}
  password: {{ required "database.password is required — set it in values.local.yaml" .Values.database.password | quote }}
